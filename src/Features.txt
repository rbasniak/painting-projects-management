Feature 1:
Add a toast on every successfull write API call.

Feature 2: 
Add a favorite currency feature, in which the user can select which currencies the uses and then all currency dropdwons will show just those currencies.

Bug 3: 
Dialog ui components do not have consistency (some have oulined controls, some not)

Bug 4:
Remove Must Have from the model creation/update? If not removed, it's not being saved
Add size in Mb to creation/update.

Bug 5:
Models list search performance is poor

Feature 3:
Add a classify button to the models list, that takes the user to the classification route for that model

Bug 6: 
When there's no image for a model, the dialog is awckaward

Feature:
Add a column for category in the materials table

Feature:
Login should be a page without layout














Quando adiciona imagem e seleciona capa, sai do dialogo, nao atualiza a capa na lista

Quando abre o Create Color Group, vem com erro de validacao e tem que clicar no dialogo pra ele reprocessar a validacao

O  dialogo Create Color Group tem dois titulus

Na hora de selecionar as zonas de cores, tem que aparecer na ordem highlight/midtones/shadows

salvar dialogo aberto e estado na rota

No dialogo de color zone management, fazer scroll separado do lado esquerdo e direito

Ao criar uma zona, ja criar ela expandida

No dialogo de color matching, qdo expandir um accordeon, fechar os outros

No dialogo de color matching, se todas as cores das zonas ja estiverem selecioada, colocar um checkmark do lado do nome da secao

No dialogo de color matching, quando selecionar uuma cor, mostrar um preview dela do lado da cor original

No dialogo de color matching, mostrar cada section em uma aba, com lazy loading se possivel

No dialogo de sumario de cores, mostrar o preview e a cor escolhida

Ao criar um projeto, colocar ele como 1o na lista em memoria

No dialogo Create Color Group abrir com foco ja no input do nome

No dialogo Create Color Zone, o delete nao aparece os botoes 